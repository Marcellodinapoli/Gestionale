CONNECTOR_PORT=8443

DB_HOST=localhost
DB_PORT=1433
DB_INSTANCE=CREDIXA_DEV
DB_NAME=CredixaDev
DB_USER=credixa_dev
DB_PASSWORD=change_me

# Slug tenant di default per health check (opzionale)
CREDIXA_TENANT_ID=demo

# Chiave API locale (header X-Connector-Key) — lasciare vuoto in dev
CONNECTOR_API_KEY=
